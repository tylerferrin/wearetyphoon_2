machine:
  timezone:
    America/Los_Angeles
    ruby:
      version: 2.2.2p95
deployment:
  production:
    branch: master
    commands:
      - npm run generate
      - surge --project ./dist --domain wearetyphoon.com
